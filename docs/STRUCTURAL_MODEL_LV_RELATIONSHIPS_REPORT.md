# 구조모델 잠재변수 간 관계 분석 보고서

**작성일**: 2025-11-05  
**프로젝트**: Sugar Substitute 2025 (대체당 연구)  
**목적**: 현재 구조모델에서 5개 잠재변수 간 관계 구조 분석

---

## 📊 핵심 발견

### ✅ **현재 구조모델 구성**

**5개 잠재변수 간 계층적 관계 구조**

```
[외생변수: 사회인구학적]
    ↓
[1차 잠재변수]
    ↓
[2차 잠재변수]
    ↓
[3차 잠재변수]
    ↓
[선택모델]
```

---

## 🎯 1. 잠재변수 간 관계 구조

### **1.1 전체 구조 다이어그램**

```
사회인구학적 변수 (외생변수)
├─ age_std (연령 표준화)
├─ gender (성별)
├─ income_std (소득 표준화)
└─ education_level (교육 수준)
    │
    ├──────────────────────────────────────┐
    │                                      │
    ↓                                      ↓
[1차 잠재변수]                        [1차 잠재변수]
건강관심도                            영양지식
(health_concern)                     (nutrition_knowledge)
= f(age, gender, income, education)  = f(age, education)
    │                                      │
    ├──────────────┐                      │
    │              │                      │
    ↓              ↓                      │
[2차 잠재변수]  [2차 잠재변수]            │
건강유익성      가격수준                  │
(perceived_     (perceived_price)        │
 benefit)       = f(income)              │
= f(health_                               │
    concern)                              │
    │              │                      │
    └──────┬───────┘                      │
           │                              │
           ↓                              │
    [3차 잠재변수]                        │
    구매의도                              │
    (purchase_intention)                  │
    = f(perceived_benefit,                │
        perceived_price)                  │
           │                              │
           └──────────────────────────────┘
                      │
                      ↓
              [선택모델]
              Choice = f(health_label, price, 
                        잠재변수들)
```

---

### **1.2 계층별 상세 분석**

#### **계층 0: 외생변수 (사회인구학적)**

| 변수 | 유형 | 설명 |
|------|------|------|
| `age_std` | 연속형 | 연령 표준화 |
| `gender` | 범주형 | 성별 (0=남성, 1=여성) |
| `income_std` | 연속형 | 소득 표준화 |
| `education_level` | 순서형 | 교육 수준 (1-6) |

---

#### **계층 1: 1차 잠재변수 (외생변수의 직접 영향)**

##### **1-1. 건강관심도 (health_concern)**

**구조방정식**:
```
health_concern = γ₁·age_std + γ₂·gender + γ₃·income_std + γ₄·education_level + η₁
```

**영향 요인**:
- ✅ `age_std`: 연령 (나이가 많을수록 건강관심도 ↑)
- ✅ `gender`: 성별 (여성이 건강관심도 ↑)
- ✅ `income_std`: 소득 (소득이 높을수록 건강관심도 ↑)
- ✅ `education_level`: 교육 (교육 수준이 높을수록 건강관심도 ↑)

**측정지표**: Q6-Q11 (6개)

**코드**:
```python
configs['health_concern'] = {
    'structural': StructuralConfig(
        sociodemographics=['age_std', 'gender', 'income_std', 'education_level']
    )
}
```

---

##### **1-2. 영양지식 (nutrition_knowledge)**

**구조방정식**:
```
nutrition_knowledge = γ₁·age_std + γ₂·education_level + η₂
```

**영향 요인**:
- ✅ `age_std`: 연령 (경험에 따른 지식 축적)
- ✅ `education_level`: 교육 (교육 수준이 높을수록 영양지식 ↑)

**측정지표**: Q30-Q49 (20개)

**코드**:
```python
configs['nutrition_knowledge'] = {
    'structural': StructuralConfig(
        sociodemographics=['age_std', 'education_level']
    )
}
```

---

#### **계층 2: 2차 잠재변수 (1차 잠재변수의 영향)**

##### **2-1. 건강유익성 (perceived_benefit)**

**구조방정식**:
```
perceived_benefit = γ₁·health_concern + η₃
```

**영향 요인**:
- ✅ `health_concern`: 건강관심도 (건강에 관심이 많을수록 대체당의 유익성을 높게 평가)

**이론적 근거**:
- 건강에 관심이 많은 사람일수록 대체당의 건강 효과를 긍정적으로 인식

**측정지표**: Q12-Q17 (6개)

**코드**:
```python
configs['perceived_benefit'] = {
    'structural': StructuralConfig(
        sociodemographics=['health_concern']  # 1차 잠재변수
    )
}
```

---

##### **2-2. 가격수준 (perceived_price)**

**구조방정식**:
```
perceived_price = γ₁·income_std + η₄
```

**영향 요인**:
- ✅ `income_std`: 소득 (소득이 높을수록 가격을 낮게 인식)

**이론적 근거**:
- 소득이 높은 사람일수록 동일한 가격을 상대적으로 저렴하게 인식

**측정지표**: Q27-Q29 (3개)

**코드**:
```python
configs['perceived_price'] = {
    'structural': StructuralConfig(
        sociodemographics=['income_std']  # 외생변수
    )
}
```

---

#### **계층 3: 3차 잠재변수 (2차 잠재변수의 영향)**

##### **3-1. 구매의도 (purchase_intention)**

**구조방정식**:
```
purchase_intention = γ₁·perceived_benefit + γ₂·perceived_price + η₅
```

**영향 요인**:
- ✅ `perceived_benefit`: 건강유익성 (유익성이 높을수록 구매의도 ↑)
- ✅ `perceived_price`: 가격수준 (가격이 낮다고 인식할수록 구매의도 ↑)

**이론적 근거**:
- **TPB (Theory of Planned Behavior)**: 태도(유익성) + 인지된 통제(가격) → 의도
- **가치-가격 모델**: 가치(유익성) > 가격 → 구매의도 ↑

**측정지표**: Q18-Q20 (3개)

**코드**:
```python
configs['purchase_intention'] = {
    'structural': StructuralConfig(
        sociodemographics=['perceived_benefit', 'perceived_price']  # 2차 잠재변수
    )
}
```

---

## 🔄 2. 잠재변수 간 인과 경로

### **2.1 직접 효과 (Direct Effects)**

| 원인 변수 | → | 결과 변수 | 경로 계수 |
|----------|---|----------|----------|
| age_std | → | health_concern | γ₁ |
| gender | → | health_concern | γ₂ |
| income_std | → | health_concern | γ₃ |
| education_level | → | health_concern | γ₄ |
| age_std | → | nutrition_knowledge | γ₅ |
| education_level | → | nutrition_knowledge | γ₆ |
| health_concern | → | perceived_benefit | γ₇ |
| income_std | → | perceived_price | γ₈ |
| perceived_benefit | → | purchase_intention | γ₉ |
| perceived_price | → | purchase_intention | γ₁₀ |

---

### **2.2 간접 효과 (Indirect Effects)**

#### **경로 1: 사회인구학적 → 건강관심도 → 건강유익성 → 구매의도**

```
age_std → health_concern → perceived_benefit → purchase_intention
간접효과 = γ₁ × γ₇ × γ₉
```

**해석**: 연령이 건강관심도를 통해 건강유익성에 영향을 주고, 최종적으로 구매의도에 영향

---

#### **경로 2: 소득 → 가격수준 → 구매의도**

```
income_std → perceived_price → purchase_intention
간접효과 = γ₈ × γ₁₀
```

**해석**: 소득이 가격 인식을 통해 구매의도에 영향

---

#### **경로 3: 교육 → 건강관심도 → 건강유익성 → 구매의도**

```
education_level → health_concern → perceived_benefit → purchase_intention
간접효과 = γ₄ × γ₇ × γ₉
```

**해석**: 교육 수준이 건강관심도를 통해 최종 구매의도에 영향

---

### **2.3 총 효과 (Total Effects)**

**예시: education_level → purchase_intention**

```
총 효과 = 직접효과 + 간접효과
       = 0 + (γ₄ × γ₇ × γ₉)
       = γ₄ × γ₇ × γ₉
```

---

## ⚠️ 3. 현재 구조의 특징 및 한계

### **3.1 특징**

#### ✅ **장점**

1. **계층적 구조**
   - 명확한 인과 관계
   - 이론적 근거 명확

2. **매개 효과 분석 가능**
   - 건강관심도 → 건강유익성 → 구매의도
   - 소득 → 가격수준 → 구매의도

3. **다중 경로**
   - 사회인구학적 변수가 여러 경로로 구매의도에 영향

---

#### ⚠️ **한계**

1. **잠재변수 간 관계가 단방향**
   - 현재: `health_concern` → `perceived_benefit` (일방향)
   - 가능성: `perceived_benefit` ↔ `health_concern` (양방향)

2. **영양지식이 독립적**
   - 현재: `nutrition_knowledge`가 다른 잠재변수와 연결 안됨
   - 가능성: `nutrition_knowledge` → `perceived_benefit`

3. **구매의도가 선택에 직접 연결 안됨**
   - 현재: `purchase_intention`이 선택모델에 포함되지 않음
   - 가능성: `purchase_intention` → `Choice`

---

### **3.2 개선 가능성**

#### **개선안 1: 영양지식 연결**

```python
# 현재
configs['perceived_benefit'] = {
    'structural': StructuralConfig(
        sociodemographics=['health_concern']
    )
}

# 개선안
configs['perceived_benefit'] = {
    'structural': StructuralConfig(
        sociodemographics=['health_concern', 'nutrition_knowledge']
    )
}
```

**이론적 근거**: 영양지식이 높을수록 대체당의 유익성을 더 잘 이해

---

#### **개선안 2: 구매의도를 선택모델에 포함**

```python
# 현재
choice_config = ChoiceConfig(
    choice_attributes=['health_label', 'price'],
    lv_in_choice=True  # 어떤 잠재변수?
)

# 개선안
choice_config = ChoiceConfig(
    choice_attributes=['health_label', 'price'],
    latent_variables=['purchase_intention'],  # 명시적 지정
    lv_in_choice=True
)
```

**이론적 근거**: TPB에서 의도는 행동의 직접적 예측변수

---

## 📊 4. 구조모델 수식 정리

### **4.1 측정모델 (Measurement Model)**

```
# 건강관심도
q6 = ζ₁·health_concern + τ₁ + ε₁
q7 = ζ₂·health_concern + τ₂ + ε₂
...
q11 = ζ₆·health_concern + τ₆ + ε₆

# 건강유익성
q12 = ζ₇·perceived_benefit + τ₇ + ε₇
...
q17 = ζ₁₂·perceived_benefit + τ₁₂ + ε₁₂

# 구매의도
q18 = ζ₁₃·purchase_intention + τ₁₃ + ε₁₃
q19 = ζ₁₄·purchase_intention + τ₁₄ + ε₁₄
q20 = ζ₁₅·purchase_intention + τ₁₅ + ε₁₅

# 가격수준
q27 = ζ₁₆·perceived_price + τ₁₆ + ε₁₆
q28 = ζ₁₇·perceived_price + τ₁₇ + ε₁₇
q29 = ζ₁₈·perceived_price + τ₁₈ + ε₁₈

# 영양지식
q30 = ζ₁₉·nutrition_knowledge + τ₁₉ + ε₁₉
...
q49 = ζ₃₈·nutrition_knowledge + τ₃₈ + ε₃₈
```

---

### **4.2 구조모델 (Structural Model)**

```
# 1차 잠재변수
health_concern = γ₁·age_std + γ₂·gender + γ₃·income_std + γ₄·education_level + η₁

nutrition_knowledge = γ₅·age_std + γ₆·education_level + η₂

# 2차 잠재변수
perceived_benefit = γ₇·health_concern + η₃

perceived_price = γ₈·income_std + η₄

# 3차 잠재변수
purchase_intention = γ₉·perceived_benefit + γ₁₀·perceived_price + η₅
```

---

### **4.3 선택모델 (Choice Model)**

```
U = β₁·health_label + β₂·price + λ·LV + ε

여기서 LV는 잠재변수 (어떤 것을 사용할지 명확하지 않음)
```

---

## ✅ 최종 결론

### **현재 구조모델 요약**

| 항목 | 내용 |
|------|------|
| **잠재변수 수** | 5개 |
| **계층 구조** | 3계층 (1차 → 2차 → 3차) |
| **외생변수** | 4개 (age, gender, income, education) |
| **직접 경로** | 10개 |
| **간접 경로** | 다수 (매개 효과) |

### **잠재변수 간 관계**

```
사회인구학적 → [건강관심도, 영양지식]
건강관심도 → 건강유익성
소득 → 가격수준
[건강유익성, 가격수준] → 구매의도
```

### **특징**

- ✅ 명확한 계층 구조
- ✅ 이론적 근거 명확
- ✅ 매개 효과 분석 가능
- ⚠️ 영양지식이 독립적
- ⚠️ 구매의도-선택 연결 불명확

---

**보고 완료** ✅  
**보고 일시**: 2025-11-05

